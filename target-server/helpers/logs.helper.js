const serverRun = (port) => {
    console.log("=========================================")
    console.log(`Server up in http://localhost:${port}/`)
    console.log("=========================================")
}

module.exports = { serverRun }